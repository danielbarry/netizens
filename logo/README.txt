# README

The `SVG` logo was created using `inkscape` on windows.

If the fonts don't appear correct when upon opening, open the `SVG` file in a text editor and check you have the relevant fonts installed.
